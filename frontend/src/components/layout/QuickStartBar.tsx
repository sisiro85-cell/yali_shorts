import { FileArrowUp, GridFour, Lightbulb, ListChecks, PlusSquare } from "@phosphor-icons/react";
const actions = [{ label: "새 프로젝트", hint: "처음부터 시작", icon: PlusSquare }, { label: "자료 불러오기", hint: "대본, 이미지, 영상", icon: FileArrowUp }, { label: "템플릿", hint: "형식 선택 후 시작", icon: GridFour }, { label: "AI 아이디어 생성", hint: "주제 제안 받기", icon: Lightbulb }, { label: "작업 큐 열기", hint: "대기 작업 확인", icon: ListChecks }];
export function QuickStartBar({ onAction }: { onAction: (label: string) => void }) { return <section className="quick-start" aria-label="빠른 시작"><h2>빠른 시작</h2><div>{actions.map((action) => { const ActionIcon = action.icon; return <button type="button" key={action.label} onClick={() => onAction(action.label)}><ActionIcon size={31} weight="regular" aria-hidden="true" /><span><strong>{action.label}</strong><small>{action.hint}</small></span></button>; })}</div></section>; }
