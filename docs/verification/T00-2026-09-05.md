# T00 검증 기록 — 테스트 수집·기준선·CI

검증일: 2026-09-05<br>
작업 기준 커밋: `76c7fb5`<br>
작업 단계: T00만 구현

## 구현 내용

- 새 `backend/tests/test_*.py`가 저장소 ignore 규칙에 가려지지 않도록 `.gitignore` 예외를 추가했다.
- 프런트 단위 테스트와 브라우저 E2E의 `no tests` 성공 옵션을 제거했다.
- Vitest가 Playwright 통합 명세를 수집하지 않도록 `e2e-integration/**`를 제외했다.
- 임시 `data_root`, fake provider, 비활성 worker를 사용하는 백엔드 QA fixture를 추가했다.
- 실제 FastAPI 서버·작업 큐·영속 JSON 저장소·fake provider를 연결하는 별도 Playwright integration 명령을 추가했다.
- Windows GitHub Actions에서 backend, frontend unit/typecheck/build/E2E/integration, render-worker 검사를 실행하도록 `QA` workflow를 추가했다.
- 테스트 범위와 실서비스 미호출 원칙을 README에 기록했다.

## TDD 및 회귀 증거

1. 신규 configuration/foundation 테스트를 먼저 실행했다.
2. 구현 전에는 fixture, 통합 명령, ignore 예외가 없어 의도한 실패가 발생했다.
3. 구현 후 configuration/foundation 테스트는 `4 passed`가 되었다.
4. 통합 명세가 Vitest에 잘못 수집되는 회귀를 테스트로 고정했다. Vitest 제외 설정 전 `1 failed`, 설정 후 `4 passed`를 확인했다.

## 최종 검증 명령과 결과

| 범위 | 명령 | 결과 |
|---|---|---:|
| Backend | `.venv\\Scripts\\python.exe -m pytest backend/tests -q` | 39 passed |
| Frontend unit | `npm --prefix frontend run unit` | 44 passed, 9 files |
| Frontend typecheck | `npm --prefix frontend run lint` | exit 0 |
| Frontend build | `npm --prefix frontend run build` | exit 0 |
| Frontend browser E2E | `npm --prefix frontend run e2e` | 24 passed |
| Real-server browser integration | `npm --prefix frontend run integration` | 1 passed |
| Render worker typecheck | `npm --prefix render-worker run lint` | exit 0 |
| Render worker unit | `npm --prefix render-worker run unit` | 10 passed |

통합 테스트는 개발 포트와 분리된 `18000`(API), `5173`(QA frontend)을 사용하고 기존 서버를 재사용하지 않는다. 브라우저에서 실제 FastAPI로 요청하고, 테스트 전용 임시 저장소에 생성 결과를 저장한 뒤 새로고침으로 복원하는 경로를 검사한다. 실 Codex 구독, 사용자 `storage`, 실제 이미지 생성, HyperFrames 렌더러는 자동 QA에서 호출하지 않는다.

## 남은 범위

- GitHub Actions workflow 자체는 로컬에서 원격 runner로 실행할 수 없으므로 원격 push 후 Actions 실행이 별도 확인 대상이다.
- 백엔드 테스트에서 Starlette/httpx 호환성 deprecation warning 1개가 남아 있지만 테스트 실패는 아니다.
- 실제 Codex MCP 이미지 생성과 로컬 렌더 완주 검증은 각각 T02, T07~T12 단계에서 수행한다.
- 다음 구현 단계는 T01이며, T00의 테스트 기반과 백업이 완료되기 전에는 T01 코드를 섞지 않는다.
